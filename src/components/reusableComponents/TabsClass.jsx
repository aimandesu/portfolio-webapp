export default class ReusableComponentsClass {
  constructor(tabTitle, content, tapableButton) {
    //new 2 things to add, most likely github porfolio if available, and another one link url to view that
    this.tabTitle = tabTitle;
    this.content = content;
    this.tapableButton = tapableButton;
    // this.index = index;
  }
}
